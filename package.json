{
  "name": "@f/object-equal",
  "description": "Shallow object equality check",
  "repository": "git://github.com/micro-js/object-equal.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.0"
  }
}
